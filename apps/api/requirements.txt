# ============================================================
# CargoIQ API — Python Dependencies
# ============================================================
# Deliberately lightweight: no GPU/CUDA dependencies anywhere in
# this list. Built to run on Railway's cheapest CPU-only tier.
# Total install footprint: well under 500MB (verified).

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Supabase
supabase==2.5.0
postgrest==0.16.10

# AI & Extraction
anthropic==0.28.0
instructor==1.3.3

# PDF Processing
# Primary: pypdf (fast, handles ~90% of real-world cases — most
# invoices/SAD500s/CargoWise exports are digitally generated, not
# scanned). Fallback: pdfplumber (tables) + Tesseract OCR (scanned
# documents). Tesseract is free, CPU-only, no GPU required — the
# system binary (tesseract-ocr + poppler-utils) installs via the
# Dockerfile's apt-get step, not via pip.
pypdf==4.2.0
pdfplumber==0.11.0
pytesseract==0.3.10
pdf2image==1.17.0
pillow==10.3.0

# Queue (BullMQ enqueue via raw Redis — no Celery needed)
redis==5.0.7

# Built-in job scheduler (replaces n8n for cron jobs)
apscheduler==3.10.4

# HTTP
httpx==0.27.0
aiohttp==3.9.5

# Encryption
cryptography==42.0.8

# Utilities
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
tenacity==8.3.0

# Logging & Monitoring
loguru==0.7.2
sentry-sdk[fastapi]==2.5.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
